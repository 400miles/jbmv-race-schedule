# JBMV 2026 Race Schedule

Race schedule app for the Jerry Baker Memorial Velodrome at Marymoor Park, Redmond WA.

## Dev

```bash
npm install
npm run dev
```

## Deploy

Deployed via Netlify. Every push to `main` triggers a rebuild automatically.

Build command: `npm run build`  
Publish directory: `dist`
